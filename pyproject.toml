[project]
name = "codetwin"
version = "0.1.10"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
# Development-only: used for running scripts locally (e.g. uv run scripts/release_pypi.py)
# The published package is generated on-the-fly by scripts/release_pypi.py into .release/python/
